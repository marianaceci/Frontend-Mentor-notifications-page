import './style.css';

function Header() {
    return (
        <header className="header">
            <h1>Notifications</h1>
            <p>3</p>
            <button type="button">Mark all as read</button>
        </header>
    );
}

export default Header;